# MEMORY — grouped by owner  (EXAMPLE — fictional)

The index, sectioned by owner. One folder per agent = that agent's private memory.
`_shared/` holds only the truly fleet-wide facts (capped). `cerebro/` holds cross-cutting
knowledge. Browse by folder; this index lists everything, one line per note.

## Shared (every agent)
<!-- identity, environment, working rules, who — capped -->
- [signing-identity](_shared/signing-identity.md) — Every external message signs as Jane Doe, Acme Corp — never as an agent; one external recipient makes the whole message external

## Alfred
- [inbox-conventions](alfred/inbox-conventions.md) — How Jane's inbox is triaged into the morning brief — labels, what gets surfaced, what gets batched

## Athena
<!-- (empty in this example) -->

## Scout
<!-- (empty in this example) -->

## Cerebro
<!-- cross-cutting knowledge; starts empty, grows by ingest -->
