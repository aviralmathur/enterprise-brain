# Who — the people who must never be misplaced

For each: **role, the channel they actually reach {{PRINCIPAL}} on, and the one thing that
gets them wrong.** A miss here is a failure of the brief, not a degradation of it. When in
doubt whether someone is leadership, treat them as if they are.

| Person | Role | Reaches {{PRINCIPAL}} on | The thing that gets it wrong |
|---|---|---|---|
| <name> | COO | Chat | Goes **first**, ahead of everyone; never batched into a group line |
| <name> | {{PRINCIPAL}}'s direct manager | Email | Naming {{PRINCIPAL}} in the body is never optional |
| <name> | Named VP / program sponsor | Email | New replies hide behind old messages on long threads — open the thread |

**Never-miss sweep (run every brief/check):**

1. Sender query, window ≥ the brief's: `{from:p1 OR from:p2 OR ...} newer_than:3d`.
2. For every thread returned, open it and read the **newest message by date** — do not
   trust the search result's message list.
3. Report every hit even if the ask lands on someone else; being named is the signal.
